'use strict'
const path = require('path');
const Halo = require(path.dirname(require.main.filename) + '/halo');

class Header extends Halo {
	GetTopMenu(URL) {
		
	}
	HTML() {

	}
}

module.exports = Header;